apiVersion: apps/v1
kind: Deployment
metadata:
  name: influxdb
spec:
  template:
    spec:
      containers:
        - name: influxdb
          resources:
            requests:
              memory: "100Mi"
              cpu: "10m"
            limits:
              memory: "8Gi"
              cpu: "1000m"
